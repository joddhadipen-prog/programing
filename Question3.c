#include <stdio.h>

int main() {
    int a, b, multiply;
    printf ("16,10: ");
    scanf ("%d %d", &a, &b);
    multiply = 16 * 10;
    printf ("mmultiplymultiply = %d/n", multiply );
    return 0;
}
